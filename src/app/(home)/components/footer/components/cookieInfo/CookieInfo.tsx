import React from 'react';
import styles from './styles.module.scss';

const CookieInfo = () => {
	return <span className={`${styles.cookieInfo}`}>We do not sell or share your personal information</span>;
};

export default CookieInfo;
