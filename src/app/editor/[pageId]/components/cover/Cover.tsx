'use client';

import React from 'react';
import styles from './styles.module.scss';

export const Cover = () => {
	if (true) return null;

	return <div className={styles.container}>{/* <img src={''} alt="Cover" className={styles.img} /> */}</div>;
};
